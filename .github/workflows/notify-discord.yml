name: notify-discord

# Invoked by publish.yml after a successful cut, or dispatched by hand for a
# backfill. Do not use on.release: GITHUB_TOKEN publish events do not start
# other workflows, so that trigger never fires for our manual release path.

on:
  workflow_call:
    inputs:
      tag:
        description: "Existing release tag to post"
        required: true
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to post"
        required: true
        type: string

permissions:
  contents: read

jobs:
  notify:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - id: release
        name: Load release notes
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ inputs.tag }}
        run: |
          name="$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json name --jq .name)"
          url="$(gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json url --jq .url)"
          delimiter="release-notes-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          {
            echo "name=$name"
            echo "url=$url"
            echo "body<<$delimiter"
            gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json body --jq .body
            echo "$delimiter"
          } >> "$GITHUB_OUTPUT"

      - name: Post release notes to Discord
        uses: SethCohen/github-releases-to-discord@24d166886aee4646d448c8a389ff9e1ebcab3682 # v1.20.0
        with:
          webhook_url: ${{ secrets.DISCORD_RELEASE_WEBHOOK_URL }}
          release_name: ${{ steps.release.outputs.name }}
          release_body: ${{ steps.release.outputs.body }}
          release_html_url: ${{ steps.release.outputs.url }}
          username: shuvcode releases
          reduce_headings: true
